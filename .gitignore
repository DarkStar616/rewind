node_modules/
# Rewind's own per-workspace control directory (snapshot repo + durable evidence store).
.rewind/
# /graphify scratch output — a knowledge-graph build artifact, never part of the product.
graphify-out/
# Build output (generated by tsup, published via each package's "files", never committed).
packages/*/dist/
# npm pack tarballs.
*.tgz
